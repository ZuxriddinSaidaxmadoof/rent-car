export enum RoleEnum {
  CLIENT = 'client',
  SUPERVISOR = 'supervisor',
  OWNER = 'owner',
  ADMIN = 'admin',
}

export enum StatusEnum {
  CREDIT = 'credit',
  DEBIT = 'debit'
}

export enum StatusTrack {
  CREATED = 'created',
  PROGRESS = 'progress',
  DONE = 'done'
}
